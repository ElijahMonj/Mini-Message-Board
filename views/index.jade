extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each message in messages
        h3 #{message.user}
        h4 #{message.text}
        h5 #{message.added}
  a(href="/new") New Message 
